# ==============================================================================
# GPU Dependencies (NVIDIA CUDA 11.8 / 12.x) - AI Challenge TP.HCM 2026
# Install on GPU servers: pip install -r requirements-gpu.txt
# ==============================================================================

# PyTorch with CUDA support (Install with index-url for GPU if needed)
# pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
setuptools>=65.0.0

# PaddlePaddle GPU (Ghim phiên bản ổn định cho CUDA 11.8/12.0)
# pip install paddlepaddle-gpu==2.6.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
paddlepaddle-gpu>=2.6.0; platform_system == "Linux"
paddleocr==2.10.0

# OCR Vietnamese Transformer
vietocr>=0.3.8

# Core Computer Vision & Processing
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0
albumentations>=1.3.0
Pillow>=9.5.0
PyYAML>=6.0
tqdm>=4.65.0

# Text Search & Indexing Storage
pyarrow>=12.0.0
pandas>=2.0.0
elasticsearch>=8.0.0
pydantic>=2.0.0

# API & Visualization Interface
streamlit>=1.30.0
fastapi>=0.100.0
uvicorn>=0.22.0
requests>=2.31.0
